// MATERIAL UI STYLES
import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  dataGridroot: {
    width: '100vw',
    height: '100vh',
    overflow: 'hidden',
  },
}))

export default useStyles