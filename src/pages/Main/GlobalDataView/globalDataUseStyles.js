// MUI STYLES
import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  globalDataRoot: {
    width: '100%',
    padding: 40,
  },
  lastUpdatedText: {
    marginBottom: 8,
  },
  itemCard: {
    height: '100%',
    padding: 16,
  },
  cardText: {
    textAlign: 'center',
  },
}))

export default useStyles