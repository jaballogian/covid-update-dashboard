// MUI STYLES
import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  globalGraphViewRoot: {
    width: '100%',
    padding: 40,
    paddingTop: 0,
  },
  itemCard: {
    height: '100%',
    padding: 16,
  },
  cardText: {
    textAlign: 'center',
  },
}))

export default useStyles